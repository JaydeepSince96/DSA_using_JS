// Traversing general meaning is fetch elements one by one inside an Arr;
// We can traverse elements in arr by using simle for loop;

function traversing() {
  const arr = [2, 3, 8, 7, 45, 6];
  for (let i = 0; i < arr.length; i++) {
    console.log(arr[i]);
  }
}
traversing();
